import { GenderTypes } from "global";

const genders: GenderTypes[] = ["male", "female", "other"];

const basicGenders: GenderTypes[] = ["male", "female"];

export default genders;
export { basicGenders };
